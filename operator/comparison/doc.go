// Package comparison hosts the primitive comparison operators (eq, ne, rx, etc.)
// that evaluate JSON paths and compare them against literal values or regular
// expressions. These operators are building blocks for higher-level filters.
package comparison
